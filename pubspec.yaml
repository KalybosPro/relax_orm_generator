name: relax_orm_generator
description: "Code generator for relax_orm — generates TableSchema from annotated classes."
version: 0.1.5
homepage: https://github.com/KalybosPro/relax_orm_generator
repository: https://github.com/KalybosPro/relax_orm_generator
issue_tracker: https://github.com/KalybosPro/relax_orm_generator/issues
documentation: https://github.com/KalybosPro/relax_orm_generator#readme
license: MIT

topics:
  - orm
  - code-generation
  - build-runner

environment:
  sdk: ">=3.11.0 <4.0.0"

platforms:
  linux:
  macos:
  windows:
  android:
  ios:

dependencies:
  build: ^4.0.5
  source_gen: ^4.2.2
  analyzer: ^10.0.1
  relax_orm: ^0.1.2

dev_dependencies:
  build_runner: ^2.4.0
  build_test: ^3.5.13
  test: ^1.25.0
  source_gen_test: ^1.0.0
  lints: ^6.1.0
