// Minimal example demonstrating the package usage.
// This example is intentionally simple — the package is a code generator.

void main() {
  // ignore: avoid_print
  print('relax_orm_generator example placeholder');
}
